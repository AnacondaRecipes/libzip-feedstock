anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: nih-at/libzip
      use_max_tag: true
      include_regex: ^v\d+(?:\.\d+)+$
      from_pattern: ^v(.+)$
      to_pattern: \1
